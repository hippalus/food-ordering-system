import {Product} from "app/models/product.model";

export class Menu {
  public id: string;
  public name: string;
  public menuItems: Product[]

  /*  public updateFrom(src: Menu): void {
     this.id = src.id;
     this.name = src.name;
     this.menuItems = src.menuItems;

   } */
}
